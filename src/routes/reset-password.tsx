import { useEffect, useState } from "react";
import { Link, createFileRoute, useNavigate } from "@tanstack/react-router";
import { supabase } from "@/integrations/supabase/client";
import { AuthShell, Field } from "@/components/auth/AuthShell";

export const Route = createFileRoute("/reset-password")({ component: ResetPasswordPage });
function ResetPasswordPage(){const navigate=useNavigate();const[pw,setPw]=useState("");const[confirm,setConfirm]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);const[ready,setReady]=useState(false);useEffect(()=>{supabase.auth.getSession().then(({data})=>setReady(Boolean(data.session)))},[]);async function submit(e:React.FormEvent){e.preventDefault();setError("");if(!ready){setError("Your reset link is missing or expired. Request a new one.");return}if(pw.length<8||pw!==confirm){setError(pw.length<8?"Use at least 8 characters.":"Passwords do not match.");return}setBusy(true);const{error}=await supabase.auth.updateUser({password:pw});if(error)setError(error.message);else await navigate({to:"/dashboard"});setBusy(false)}return <AuthShell title="Choose a new password" subtitle="Set a new password for your Aether account."><form onSubmit={submit} className="mt-8 space-y-5"><Field label="New password" value={pw} onChange={setPw} type="password" autoComplete="new-password"/><Field label="Confirm password" value={confirm} onChange={setConfirm} type="password" autoComplete="new-password"/>{error&&<p className="rounded-xl border border-red-400/20 bg-red-400/5 px-4 py-3 text-sm text-red-200">{error}</p>}<button disabled={busy} className="w-full rounded-xl bg-[#d7e5b0] px-4 py-3.5 text-sm font-semibold text-[#10150f]">{busy?"Updating…":"Update password"}</button><p className="text-center text-xs text-white/40"><Link to="/login" className="text-white/70">Back to sign in</Link></p></form></AuthShell>}
