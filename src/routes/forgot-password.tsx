import { useState } from "react";
import { Link, createFileRoute } from "@tanstack/react-router";
import { supabase } from "@/integrations/supabase/client";
import { AuthShell, Field } from "@/components/auth/AuthShell";

export const Route = createFileRoute("/forgot-password")({ component: ForgotPasswordPage });
function ForgotPasswordPage(){const[email,setEmail]=useState("");const[error,setError]=useState("");const[notice,setNotice]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");setNotice("");const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});if(error)setError(error.message);else setNotice("If an account exists for that address, a password reset link has been sent.");setBusy(false)}return <AuthShell title="Reset your password" subtitle="We will send a secure reset link to your account email."><form onSubmit={submit} className="mt-8 space-y-5"><Field label="Email" value={email} onChange={setEmail} type="email" autoComplete="email" placeholder="you@example.com"/>{error&&<p className="rounded-xl border border-red-400/20 bg-red-400/5 px-4 py-3 text-sm text-red-200">{error}</p>}{notice&&<p className="rounded-xl border border-[#d7e5b0]/20 bg-[#d7e5b0]/5 px-4 py-3 text-sm text-[#d7e5b0]">{notice}</p>}<button disabled={busy} className="w-full rounded-xl bg-[#d7e5b0] px-4 py-3.5 text-sm font-semibold text-[#10150f]">{busy?"Sending…":"Send reset link"}</button><p className="text-center text-xs text-white/40"><Link to="/login" className="text-white/70 hover:text-white">Back to sign in</Link></p></form></AuthShell>}
