import{useEffect,useState}from"react";import{createFileRoute,Link}from"@tanstack/react-router";import{ArrowLeft,Settings2}from"lucide-react";import{supabase}from"@/integrations/supabase/client";import{getRole}from"@/lib/auth";
export const Route=createFileRoute("/admin/settings")({component:Settings});function Settings(){const[role,setRole]=useState("");useEffect(()=>{supabase.auth.getUser().then(async({data})=>{if(data.user)setRole(await getRole(data.user.id))})},[]);return <main className="min-h-screen bg-[#070907] text-[#f4f3ee]"><div className="mx-auto max-w-5xl px-6 py-10 lg:px-10"><Link to="/admin" className="inline-flex items-center gap-2 text-sm text-white/35"><ArrowLeft className="h-4 w-4"/> Control plane</Link><div className="mt-10 flex items-center gap-3"><Settings2 className="h-5 w-5 text-[#d7e5b0]"/><h1 className="text-3xl">Settings</h1></div><div className="mt-8 rounded-2xl border border-white/8 bg-white/[.025] p-6"><div className="grid gap-5 sm:grid-cols-2"><div><div className="text-xs uppercase tracking-[.14em] text-white/25">Current role</div><div className="mt-2 text-sm">{role||"Checking…"}</div></div><div><div className="text-xs uppercase tracking-[.14em] text-white/25">MCP endpoint</div><div className="mt-2 font-mono text-sm text-[#d7e5b0]/70">/api/mcp</div></div><div><div className="text-xs uppercase tracking-[.14em] text-white/25">Storage</div><div className="mt-2 text-sm">Private object storage · project-blobs</div></div><div><div className="text-xs uppercase tracking-[.14em] text-white/25">Admin bootstrap</div><div className="mt-2 text-sm">Server-side environment configuration</div></div></div><p className="mt-8 text-xs leading-5 text-white/25">Credential values and service-role secrets are intentionally unavailable to this UI. Configuration changes that affect infrastructure should be made through protected deployment secrets and reviewed/audited.</p></div></div></main>}
