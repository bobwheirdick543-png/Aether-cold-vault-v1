import { useState } from "react";
import { createFileRoute, useNavigate, Link } from "@tanstack/react-router";
import { supabase } from "@/integrations/supabase/client";
import { Logo } from "@/components/brand/logo";
import { Shield } from "lucide-react";

export const Route=createFileRoute("/admin/login")({component:AdminLogin});
function AdminLogin(){const[email,setEmail]=useState("");const[pw,setPw]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);const navigate=useNavigate();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const{data,error}=await supabase.auth.signInWithPassword({email,password:pw});if(error){setError(error.message);setBusy(false);return}if(!data.user){setError("Authentication failed.");setBusy(false);return}const{data:roles}=await supabase.from("user_roles").select("role").eq("user_id",data.user.id);const allowed=roles?.some(r=>r.role==="admin"||r.role==="platform_owner");if(!allowed){await supabase.auth.signOut();setError("This account does not have platform administration access.");setBusy(false);return}await navigate({to:"/admin"});setBusy(false)}return <main className="min-h-screen bg-[#070907] px-6 py-10 text-[#f4f3ee]"><div className="mx-auto max-w-md"><Link to="/" className="inline-flex"><Logo showWordmark/></Link><section className="mt-20 rounded-[28px] border border-[#d7e5b0]/15 bg-[#0c110d] p-8 shadow-2xl"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#d7e5b0]/10 text-[#d7e5b0]"><Shield className="h-5 w-5"/></div><h1 className="mt-7 text-3xl tracking-[-.03em]">Platform control</h1><p className="mt-2 text-sm leading-6 text-white/40">A separate administrative entrance for Aether Code Vault.</p><form onSubmit={submit} className="mt-8 space-y-4"><input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="username" placeholder="Admin email" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-sm outline-none focus:border-[#d7e5b0]/50"/><input value={pw} onChange={e=>setPw(e.target.value)} type="password" autoComplete="current-password" placeholder="Password" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-sm outline-none focus:border-[#d7e5b0]/50"/>{error&&<p className="rounded-xl border border-red-400/20 bg-red-400/5 px-4 py-3 text-sm text-red-200">{error}</p>}<button disabled={busy} className="w-full rounded-xl bg-[#d7e5b0] px-4 py-3.5 text-sm font-semibold text-[#10150f]">{busy?"Verifying…":"Enter control plane"}</button></form></section></div></main>}
